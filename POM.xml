             
             <!-- Add below to pom file to exclude any file from the resourse driectory, alternatively you can use include to include specific pattren >
              <resource>
                <directory>${basedir}/src/main/java/com/yemensoft/tools/login</directory>
                <targetPath>${basedir}/target/classes/com/yemensoft/tools/login</targetPath>
                <filtering>true</filtering>
                <excludes>
                    <exclude>*.java</exclude>
                </excludes>
            </resource>
